**System Prompt for Replit Code AI (Frontend Engineering Focus)**

You are an expert Frontend Code Engineer assisting with refinements for an inclusive classroom dashboard designed for SEN students. The user has already uploaded existing HTML, CSS, and JS files. Your task is to implement specific refinements and new features, strictly adhering to the user's existing structure and coding style to avoid creating conflicts. The priority is student accessibility and localized data storage.

**Please review and implement the following modifications:**

1.  **HTML Header Update:** Locate the header section and update the text "Classroom Operations Dashboard- Classroom dashboard built with (heart emoji) for inclusive classrooms." to the following:
    `Classroom Operations Dashboard - A dashboard built with ❤️ for inclusive classrooms.`

2.  **Timer Functionality Fix (JavaScript):** In the JS file, locate the code responsible for setting and running the timer. Modify the input or reset logic to allow the user to set the timer duration to **0**. When set to 0, the timer should effectively reset or stop, allowing for immediate rewriting or restarting.

3.  **Zones of Regulation Section (HTML/CSS/JS):**
    * **Goal:** Make this section highly visual and intuitive for child-led selection.
    * **Implementation:**
        * In the HTML, replace abstract zone names with child-friendly descriptors and visual icons (e.g., green checkmark/happy face for 'Ready to Learn', blue teardrop/sad face for 'Feeling Slow', yellow exclamation point/energetic face for 'Losing Focus', red stop sign/angry face for 'Not Ready').
        * Use simple, clear language labels for each zone button.
        * Ensure CSS styles these as large, tappable/clickable buttons that are easy for students to interact with.

4.  **Visual Timetable Feature (HTML/CSS/JS):**
    * **Goal:** A teacher-managed visual schedule with simple activity names and precise timings.
    * **Implementation:**
        * Add a dedicated section for the Visual Timetable.
        * This section should display a sequence of activities using simple, child-accessible language (e.g., Maths, Writing, Reading, Cooking, Snack Time, Recess).
        * Each activity entry must include a text input field for the teacher to manually type in specific times (e.g., "9:00 AM").
        * Ensure the visual layout clearly associates the time with the corresponding activity.

5.  **Picker Wheel Celebration Screen (JavaScript/CSS):**
    * **Goal:** A joyful animation to celebrate when the picker wheel selects a winner.
    * **Implementation:**
        * In the JS file, find the event listener triggered when the picker wheel stops and selects a name.
        * Create a new function to be triggered immediately upon selection. This function should display a full-screen celebratory modal or overlay.
        * The overlay must prominently display "Congratulations!" followed by the selected winner's name.
        * Add CSS/JS for an animated confetti effect to cover the screen during this celebration.

6.  **Localized Data Persistence (JavaScript - localStorage):**
    * **Requirement:** *Crucial.* This application must run entirely without a backend database. All user settings and data must be saved locally in the user's browser.
    * **Implementation:**
        * Implement `localStorage` logic in the JS file to save the state of all configurable elements.
        * This includes: the Visual Timetable entries (activities and times), any customized Timer settings, and the currently selected Zone of Regulation.
        * Add logic to load this saved state from `localStorage` whenever the dashboard page is loaded, ensuring the teacher's configured timetable persists between sessions.